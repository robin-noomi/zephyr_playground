sample:
  description: firmware-nordic-nrf52, the accelerometer and microphone Edge Impulse example for Zephyr
    application
  name: firmware-nordic-nrf52
common:
    tags: introduction
    filter: dt_enabled_alias_with_parent_compat("led0", "gpio-leds")
    depends_on: gpio
    harness: console, led
    harness_config:
      type: one_line
      regex:
        - "Hello World! (.*)"